# Generated by Toneforge
# Pallet: Base2Tone
# Uno hue: 20
# Duo hue: 347

# -------------------------------------------------------------------
# Syntax
# -------------------------------------------------------------------

attribute = "accent-strong"

keyword = "accent-dark"
"keyword.directive" = "accent"

namespace = "foreground-strong"

punctuation = "foreground-dim"
"punctuation.delimiter" = "foreground-dim"

operator = "accent-muted"
special = "accent-bright"

variable = "foreground"
"variable.parameter" = { fg = "foreground-strong" }
"variable.other.member" = "foreground-bright"
"variable.builtin" = "accent-max"

type = "foreground-max"
"type.builtin" = "accent-pale"

constructor = "accent-strong"

function = "foreground-max"
"function.builtin" = "accent-pale-max"
"function.macro" = "accent-strong"

tag = "accent"

comment = { fg = "muted", modifiers = ["italic"] }

constant = "accent-light"
"constant.builtin" = "accent-pale"
"constant.numeric" = "accent-muted"
"constant.character.escape" = "accent-bright"

string = "accent-pale-max"

label = "accent-strong"

tabstop = { modifiers = ["italic"], bg = "surface-raised" }


# -------------------------------------------------------------------
# Markup
# -------------------------------------------------------------------

"markup.heading" = "accent-strong"

"markup.bold" = { modifiers = ["bold"] }
"markup.italic" = { modifiers = ["italic"] }
"markup.strikethrough" = { modifiers = ["crossed_out"] }

"markup.link.url" = { fg = "accent-pale", modifiers = ["underlined"] }
"markup.link.text" = "accent"
"markup.raw" = "accent-pale-max"


# -------------------------------------------------------------------
# Diff
# -------------------------------------------------------------------

"diff.plus" = "accent"
"diff.minus" = "error"
"diff.delta" = "info"


# -------------------------------------------------------------------
# Interface
# -------------------------------------------------------------------

"ui.background" = { bg = "background" }
"ui.background.separator" = { fg = "border" }

"ui.linenr" = { fg = "muted" }
"ui.linenr.selected" = { fg = "foreground-bright" }

"ui.statusline" = { fg = "foreground-strong", bg = "surface-raised" }
"ui.statusline.inactive" = { fg = "foreground-dim", bg = "surface" }

"ui.statusline.normal" = { fg = "background", bg = "foreground-strong" }
"ui.statusline.insert" = { fg = "background", bg = "accent-strong" }
"ui.statusline.select" = { fg = "background", bg = "accent-bright" }

"ui.popup" = { fg = "foreground", bg = "surface" }

# Popup/menu/window border
"ui.window" = { fg = "border" }

"ui.help" = { fg = "foreground-max", bg = "surface-raised" }

"ui.text" = { fg = "foreground" }
"ui.text.focus" = { fg = "foreground-max" }
"ui.text.inactive" = "muted"
"ui.text.directory" = { fg = "accent-strong" }

"ui.virtual" = { fg = "muted" }
"ui.virtual.ruler" = { bg = "surface-raised" }
"ui.virtual.indent-guide" = { fg = "border" }
"ui.virtual.jump-label" = { fg = "accent-max", modifiers = ["bold"] }


# -------------------------------------------------------------------
# Selection / Cursor
# -------------------------------------------------------------------

"ui.selection" = { bg = "selection" }
"ui.selection.primary" = { bg = "selection-strong" }

"ui.cursor" = { modifiers = ["reversed"] }

"ui.cursor.insert" = { bg = "foreground-max" }
"ui.cursor.select" = { bg = "accent-bright" }

"ui.cursor.primary.insert" = { bg = "foreground-max" }
"ui.cursor.primary.select" = { bg = "accent-bright" }

"ui.cursor.match" = { fg = "background", bg = "accent-max" }

"ui.cursorline.primary" = { bg = "surface-raised" }

"ui.highlight" = { bg = "selection" }
"ui.highlight.frameline" = { bg = "surface-active" }


# -------------------------------------------------------------------
# Completion / Menus
# -------------------------------------------------------------------

"ui.menu" = { fg = "foreground", bg = "surface" }

"ui.menu.selected" = { fg = "background", bg = "accent-pale-max"}

"ui.menu.scroll" = {fg = "foreground-strong",bg = "border"}


# -------------------------------------------------------------------
# Diagnostics
# -------------------------------------------------------------------

"diagnostic.hint" = {underline = {color = "hint", style = "curl"}}

"diagnostic.info" = {underline = {color = "info", style = "curl"}}

"diagnostic.warning" = {underline = {color = "warning", style = "curl"}}

"diagnostic.error" = {underline = {color = "error", style = "curl"}}

"diagnostic.unnecessary" = {modifiers = ["dim"]}

"diagnostic.deprecated" = {modifiers = ["crossed_out"]}

warning = "warning"
error = "error"
info = "info"
hint = "hint"


# -------------------------------------------------------------------
# Debugging
# -------------------------------------------------------------------

"ui.debug" = { fg = "accent-light" }
"ui.debug.breakpoint" = { fg = "error" }


# -------------------------------------------------------------------
# Palette
# -------------------------------------------------------------------

[palette]

# -------------------------
# Background / UI hierarchy
# -------------------------

# Main editor background
background = "#332924"

# Dark popup/menu/statusline background
surface = "#42352f"

# Slightly brighter UI surface
surface-raised = "#68544b"

# Strong active UI surface
surface-active = "#836d63"

# Main selection background
selection = "#756157"

# Stronger primary selection
selection-strong = "#836d63"

# Borders, separators, indent guides
border = "#978278"

# Comments, inactive text, line numbers
muted = "#a79890"


# -------------------------
# Uno foreground hierarchy
# -------------------------

# Lowest-emphasis foreground
foreground-dim = "#f5a075"

# Normal editor text / variables
foreground = "#febfa0"

# Stronger foreground
foreground-strong = "#fed0b9"

# High-emphasis foreground
foreground-bright = "#fee1d2"

# Maximum Uno brightness
foreground-max = "#fff1eb"


# -------------------------
# Subdued Duo hierarchy
# -------------------------

# Deep secondary accent
accent-deep = "#9f9396"

# Muted Duo accent
accent-muted = "#b0a6a8"

# Soft Duo accent
accent-soft = "#c2b7ba"

# Light Duo accent
accent-light = "#d7cbce"

# Pale Duo accent
accent-pale = "#eae1e3"

# Lightest subdued Duo accent
accent-pale-max = "#fbf9f9"


# -------------------------
# Vivid Duo hierarchy
# -------------------------

# Dark vivid accent, useful for keywords
accent-dark = "#ed5e7d"

# Main accent
accent = "#f8728f"

# Strong accent
accent-strong = "#fc839d"

# Bright accent
accent-bright = "#ff99af"

# Maximum vivid accent
accent-max = "#ffb2c3"


# -------------------------
# Semantic diagnostic roles
# -------------------------

# Errors / removals
error = "#c2b7ba"

# Warnings
warning = "#ff99af"

# Informational diagnostics
info = "#fee1d2"

# Hints
hint = "#eae1e3"